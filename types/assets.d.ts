// Static asset import types (e.g. `import icon from "@/assets/icons/x.svg"`).
// next-env.d.ts also references this, but it is gitignored and absent on fresh
// checkouts (CI runs `tsc --noEmit` before any Next.js build generates it).
/// <reference types="next/image-types/global" />
